version: "3.8"
services:
  web:
    build: .
    ports:
      - "80:80"
    environment:
      - DEBUG=0
      - SECRET_KEY=changeme123456   
      - ALLOWED_HOSTS=0.0.0.0
    volumes:
      - .:/app     
    restart: unless-stopped

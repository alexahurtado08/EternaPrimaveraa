version: "3.8"
services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DEBUG=0
      - SECRET_KEY=changeme123456   # reemplaza por un secreto real en producción
      - ALLOWED_HOSTS=0.0.0.0
    volumes:
      - .:/app     # útil para desarrollo (si prefieres que el contenedor use el código local)
    restart: unless-stopped
